![](/banner.png)
# 🔒 WinLock (Python Lock Screen)

**WinLock** — учебный проект полноэкранного **lock screen для Windows**, реализованный на **Python + Tkinter**.
Приложение блокирует интерфейс, принимает пароль, ограничивает количество попыток и поддерживает сборку в `.exe`.

> ⚠️ Проект предназначен **исключительно для образовательных и демонстрационных целей**.

>>⚠️ Предупреждение Windows SmartScreen
>>Данное приложение не имеет цифровой подписи.
>>При запуске исполняемого файла, загруженного с GitHub,
>>Windows Defender SmartScreen может показать предупреждение.
>>Чтобы запустить приложение:
>>нажмите «Подробнее» → «Выполнить в любом случае».
>>Это стандартное поведение Windows для неподписанных приложений.


---

## ✨ Возможности

* Полноэкранный режим
* Окно всегда поверх других
* Скрытый ввод пароля (`*`)
* Проверка пароля через **SHA-256**
* Ограничение количества попыток
* Временная блокировка после ошибок
* Отсутствие консольного окна в `.exe`
* Поддержка пользовательской иконки

---

## 🧠 Как это работает

* Пароль **не хранится в открытом виде**
* В коде используется **хэш SHA-256**
* После `MAX_ATTEMPTS` попыток:

  * ввод блокируется
  * запускается таймер (`LOCK_TIME`)
* После успешного ввода:

  * приложение закрывается

---

## 📁 Структура проекта

```
WinLock/
├── winlock.py           # основной файл lock screen
├── hash_password.py     # утилита для генерации SHA-256 хэша
├── output-0.ico         # иконка приложения (.ico)
├── README.md
```

---

## ⚙️ Требования

* **Windows 10 / 11**
* **Python 3.9+**
* `pip`

Проверка версии Python:

```bat
python --version
```

---

## ▶️ Запуск из Python

```bat
python winlock.py
```

---

## 🔐 Настройка пароля

### 1️⃣ Сгенерировать хэш пароля

Запусти:

```bat
python hash_password.py
```

Введи пароль:

```
Enter password to hash: 2007
```

Пример результата:

```
Hashed password: ef797c8118f02dfb649607dd5d3f8c7623048c9c063d532cc95c5ed7a898a64f
```

---

### 2️⃣ Установить хэш в `winlock.py`

```python
PASSWORD_HASH = "ef797c8118f02dfb649607dd5d3f8c7623048c9c063d532cc95c5ed7a898a64f"
```

> ❗ Никогда не храните пароль в открытом виде в коде.

---

## 📦 Сборка `.exe` (PyInstaller)

### Установка PyInstaller

```bat
pip install pyinstaller
```

---

### Сборка без консоли

```bat
pyinstaller --onefile --noconsole winlock.py
```

---

### Сборка с именем и иконкой (рекомендуется)

```bat
pyinstaller --onefile --noconsole --name LockScreen --icon=lock.ico winlock.py
```

Результат:

```
dist/LockScreen.exe
```

---

## 🎨 Требования к иконке `.ico`

Иконка должна содержать **несколько размеров**:

```
16x16
32x32
48x48
64x64
128x128
256x256
```

Рекомендуемый исходник:

* PNG 512×512
* Прозрачный фон

---

## 🧹 Очистка перед пересборкой

Если возникают проблемы с иконкой или сборкой:

```bat
rmdir /s /q build
rmdir /s /q dist
del LockScreen.spec
```

---

## ❗ Ограничения проекта

* Это **не системная защита Windows**
* Можно завершить через:

  * диспетчер задач
  * перезагрузку
* Не предназначено для защиты чужих компьютеров

---

## 📌 Назначение проекта

Проект создан для:

* практики **Tkinter**
* понимания **GUI-архитектуры**
* изучения **PyInstaller**
* демонстрации работы с хэшированием

---

## 👨‍💻 Автор

**Akrom**
Python Developer · GUI / Security Practice

---

## 📄 Лицензия

MIT License (или любая другая по желанию)

---

⚠️ Security Notice

This application runs in user space and does not replace
Windows authentication mechanisms.

Shutting down or restarting the computer will stop the application.
For real security, always use:
- Windows account password
- BitLocker disk encryption
- BIOS/UEFI password
